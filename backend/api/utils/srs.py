# Returns words due for SRS, ordered by deadline. 
# Right now this is a stub because we don't have an SRS algorithm yet. 
def get_words_due_for_review(user_id: str, language_id: str) -> list[str]:
    return []
